#include "Entity.h"

gea::Entity::~Entity()
{
    /*
    //Upon deleting an entity, loop through all of its components
    for (auto component : mComponents)
    {
        //Find last component of same types
        //Update that components location in the vector storage to the current component's index
        //Reduce the nextID value for this component type by 1
    }*/
}
